import logging

# Filename with feed URLs, one per line.
feed_url_filename = 'feed_urls'
# Port to listen to callbacks from the publisher.
local_port = 80
update_secs = 60 * 10
log_level = logging.WARNING
